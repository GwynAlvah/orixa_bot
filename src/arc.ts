export class ArcClient{constructor(private url:string){}async balance(contract:string,wallet:string){const data="0x70a08231"+wallet.slice(2).padStart(64,"0");const r=await fetch(this.url,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({jsonrpc:"2.0",id:1,method:"eth_call",params:[{to:contract,data},"latest"]}),signal:AbortSignal.timeout(10000)});if(!r.ok)throw Error("Arc Testnet RPC HTTP "+r.status);const p=await r.json()as{result?:string;error?:{message?:string}};if(!p.result)throw Error(p.error?.message||"Invalid RPC response");const n=BigInt(p.result);if(n>BigInt(Number.MAX_SAFE_INTEGER))throw Error("Balance too large");return Number(n)}}
