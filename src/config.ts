import "dotenv/config";
const req=(n:string)=>{const v=process.env[n]?.trim();if(!v)throw Error("Missing required environment variable: "+n);return v};
export const config={discordToken:req("DISCORD_TOKEN"),guildId:req("DISCORD_GUILD_ID"),openSeaApiKey:req("OPENSEA_API_KEY"),arcRpcUrl:process.env.ARC_RPC_URL?.trim()||"https://rpc.mainnet.arc.io",arcChainId:Number(process.env.ARC_CHAIN_ID||5042),orixaContractAddress:"0x525f3d66c782e6f191213bdc031fd2b56c5c3a69".toLowerCase(),verificationTtlMs:Number(process.env.VERIFICATION_TTL_MINUTES||15)*60000,holderRoleSyncIntervalMs:Number(process.env.HOLDER_ROLE_SYNC_INTERVAL_MINUTES||30)*60000};
