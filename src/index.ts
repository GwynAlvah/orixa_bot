import{randomBytes}from"node:crypto";import{ActionRowBuilder,ButtonBuilder,ButtonStyle,ChannelType,Client,EmbedBuilder,Events,GatewayIntentBits,Interaction,MessageFlags,ModalBuilder,PermissionFlagsBits,TextInputBuilder,TextInputStyle}from"discord.js";import{config}from"./config.js";import{OpenSeaClient}from"./opensea.js";import{ArcClient}from"./arc.js";import{VerificationStore}from"./store.js";
const VERIFY="holder:verify",MODAL="holder:wallet-modal",INPUT="holder:wallet-address",CONFIRM="holder:confirm",ADDRESS=/^0x[a-fA-F0-9]{40}$/;const client=new Client({intents:[GatewayIntentBits.Guilds]}),store=new VerificationStore("data/verifications.json"),openSea=new OpenSeaClient(config.openSeaApiKey,"",""),chain=new ArcClient(config.arcTestnetRpcUrl);
client.once(Events.ClientReady,c=>{console.log("Logged in as "+c.user.tag)});client.on(Events.InteractionCreate,async i=>{try{await handle(i)}catch(e){console.error(e);if(i.isRepliable()){const x={content:"Verification failed. Try again later.",flags:MessageFlags.Ephemeral as const};i.replied||i.deferred?await i.followUp(x):await i.reply(x)}}});
async function handle(i:Interaction){if(i.isChatInputCommand()&&i.commandName==="setup-verification"){if(!i.inGuild()||!i.memberPermissions?.has(PermissionFlagsBits.ManageGuild)){await i.reply({content:"Manage Server permission is required.",flags:MessageFlags.Ephemeral});return}const channel=i.options.getChannel("channel",true,[ChannelType.GuildText]),contract=config.orixaContractAddress;const tiers:Array<{roleId:string;nftCount:number}>=[];for(let n=1;n<=5;n++){const role=i.options.getRole("role-"+n),count=i.options.getInteger("nft-count-"+n);if((role&&count===null)||(!role&&count!==null)){await i.reply({content:"Tier "+n+" needs both role and NFT count.",flags:MessageFlags.Ephemeral});return}if(role&&count!==null)tiers.push({roleId:role.id,nftCount:count})}if(new Set(tiers.map(t=>t.roleId)).size!==tiers.length||new Set(tiers.map(t=>t.nftCount)).size!==tiers.length){await i.reply({content:"Roles and thresholds must be unique.",flags:MessageFlags.Ephemeral});return}tiers.sort((a,b)=>a.nftCount-b.nftCount);const me=i.guild!.members.me,bad=tiers.find(t=>{const r=i.guild!.roles.cache.get(t.roleId);return!r||r.managed||!me||r.position>=me.roles.highest.position});if(bad){await i.reply({content:"Move my bot role above <@&"+bad.roleId+">.",flags:MessageFlags.Ephemeral});return}await store.setGuildSetup(i.guildId,{channelId:channel.id,contractAddress:contract,tiers});const lines=tiers.map(t=>"- <@&"+t.roleId+">: **"+t.nftCount+"+ NFTs**").join("\n"),embed=new EmbedBuilder().setColor(0x57f287).setTitle("Verify Orixa holder status").setDescription(["Click **Verify holder** and submit your wallet address.","Add the temporary code to your OpenSea profile bio, then confirm.","","**Contract**: "+contract,"**Arc Testnet**","","**Role tiers**",lines,"","No transaction, approval, seed phrase, or private key is requested."].join("\n")),row=new ActionRowBuilder<ButtonBuilder>().addComponents(new ButtonBuilder().setCustomId(VERIFY).setLabel("Verify holder").setStyle(ButtonStyle.Success));await channel.send({embeds:[embed],components:[row]});await i.reply({content:"Panel posted in <#"+channel.id+">.",flags:MessageFlags.Ephemeral});return}
if(i.isButton()&&i.customId===VERIFY){const m=new ModalBuilder().setCustomId(MODAL).setTitle("NFT holder verification"),x=new TextInputBuilder().setCustomId(INPUT).setLabel("Your EVM wallet address").setPlaceholder("0x...").setStyle(TextInputStyle.Short).setMinLength(42).setMaxLength(42).setRequired(true);m.addComponents(new ActionRowBuilder<TextInputBuilder>().addComponents(x));await i.showModal(m);return}
if(i.isModalSubmit()&&i.customId===MODAL){const wallet=i.fields.getTextInputValue(INPUT).trim().toLowerCase();if(!ADDRESS.test(wallet)){await i.reply({content:"Invalid EVM wallet address.",flags:MessageFlags.Ephemeral});return}const owner=store.walletOwner(wallet);if(owner&&owner!==i.user.id){await i.reply({content:"Wallet already verified by another account.",flags:MessageFlags.Ephemeral});return}const code="ORIXA-"+randomBytes(5).toString("hex").toUpperCase();await store.setPending({discordUserId:i.user.id,walletAddress:wallet,code,expiresAt:Date.now()+config.verificationTtlMs});const row=new ActionRowBuilder<ButtonBuilder>().addComponents(new ButtonBuilder().setCustomId(CONFIRM).setLabel("Check OpenSea profile").setStyle(ButtonStyle.Primary));await i.reply({content:"Add this code to the OpenSea bio for "+wallet+":\n\n**"+code+"**\n\nSave it, then press below.",components:[row],flags:MessageFlags.Ephemeral});return}
if(i.isButton()&&i.customId===CONFIRM){await i.deferReply({flags:MessageFlags.Ephemeral});const p=store.getPending(i.user.id);if(!p){await i.editReply("No pending verification. Start again.");return}if(p.expiresAt<Date.now()){await store.removePending(i.user.id);await i.editReply("Code expired. Start again.");return}if(store.walletOwner(p.walletAddress)&&store.walletOwner(p.walletAddress)!==i.user.id){await i.editReply("Wallet already claimed.");return}if(!await openSea.profileContainsCode(p.walletAddress,p.code)){await i.editReply("Code is not visible in the OpenSea bio yet.");return}if(!i.inGuild()||i.guildId!==config.guildId){await i.editReply("Use the configured server.");return}const setup=store.getGuildSetup(i.guildId);if(!setup){await i.editReply("Ask an admin to run /setup-verification.");return}const count=await chain.balance(setup.contractAddress,p.walletAddress),qualified=setup.tiers.filter(t=>count>=t.nftCount);if(!qualified.length){await i.editReply("Wallet holds "+count+" NFT(s), below the minimum.");return}const member=await i.guild!.members.fetch(i.user.id),all=setup.tiers.map(t=>t.roleId),add=qualified.map(t=>t.roleId),remove=all.filter(r=>member.roles.cache.has(r)&&!add.includes(r));if(remove.length)await member.roles.remove(remove,"NFT tier sync");await member.roles.add(add,"Verified Orixa holder");await store.markVerified(p);await i.editReply("Verified with "+count+" NFT(s). Granted: "+add.map(r=>"<@&"+r+">").join(", ")+".")}}
await store.initialize();await client.login(config.discordToken);
